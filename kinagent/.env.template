# Azure Container Registry Configuration
ACR_REGISTRY=obscr.azurecr.io
ACR_REPO=kagent-dev/kagent
ACR_USERNAME=your-acr-username
ACR_PASSWORD=your-acr-password

# AKS Deployment Configuration
AKS_NAMESPACE=kagent
AKS_SERVICE_TYPE=ClusterIP
AKS_DEFAULT_MODEL_PROVIDER=azureOpenAI

# =============================================================================
# Image Tag Configuration
# =============================================================================
# By default, tags are auto-generated from git directory hashes (smart mode).
# This enables automatic change detection: only changed components are rebuilt.
#
# To use smart deploys, just run: make aks-smart-deploy
# To preview what will be built: make aks-show-pending-changes
#
# Set USE_GLOBAL_TAG=true to use IMAGE_TAG for all components (legacy behavior)
USE_GLOBAL_TAG=

# Global image tag (only used if USE_GLOBAL_TAG=true)
# Find available tags: make acr-list-tags
IMAGE_TAG=

# Per-component tags (optional - auto-generated from git hash if empty)
# Override these only when you need manual control over specific component versions
# Example: CONTROLLER_IMAGE_TAG=abc12345 to pin controller to a specific version
CONTROLLER_IMAGE_TAG=
UI_IMAGE_TAG=
APP_IMAGE_TAG=
KAGENT_ADK_IMAGE_TAG=
DATABRICKS_MCP_IMAGE_TAG=

# Azure OpenAI Configuration
AZUREOPENAI_API_KEY=your-azure-openai-api-key
AZUREOPENAI_ENDPOINT=https://your-resource.openai.azure.com/
AZUREOPENAI_DEPLOYMENT=your-deployment-name
AZUREOPENAI_API_VERSION=2024-08-01-preview
AZUREOPENAI_MODEL=gpt-4o

# OpenTelemetry Configuration
OTEL_ENDPOINT=http://otel-collector.merlin-dev.svc.cluster.local:4317

# Optional: Other LLM Provider API Keys (only needed if using those providers)
# OPENAI_API_KEY=your-openai-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key
# GOOGLE_API_KEY=your-google-api-key

# Databricks DataSource Configuration (optional)
# When set, a DataSource will be created automatically.
# For multiple DataSources, configure them in helm values or create via kubectl.
# See contrib/examples/datasource-databricks.yaml for manifest examples.
DATABRICKS_DATASOURCE_NAME=default-databricks
DATABRICKS_TOKEN=your-databricks-personal-access-token
DATABRICKS_WORKSPACE_URL=https://your-workspace.cloud.databricks.com
DATABRICKS_CATALOG=main
DATABRICKS_SCHEMA=
DATABRICKS_WAREHOUSE_ID=
